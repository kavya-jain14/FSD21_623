# Portfolio using Express and HTML

A responsive personal portfolio served through Express.

```bash
npm install
npm start
```

Open `http://localhost:3000` in a browser.

Run `npm test` to verify the portfolio page and profile endpoint.
