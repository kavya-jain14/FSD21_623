# React and Express

A complete full stack workshop tracker. React manages the interface while Express provides the REST API.

## Run

```bash
npm install
npm run dev
```

Open `http://localhost:5173`. The Express API runs on `http://localhost:4000`.

## Verify

```bash
npm test
npm run build
```

